name: CD - Go

run-name: Publish Go ${{ github.ref }}

on:
  workflow_dispatch:
  push:
    tags:
      - '*.*.*'
      - '!v*'

jobs:
  gen:
    uses: katastroma/skeuotheke/.github/workflows/release-buf-proto.yaml@v0
    permissions:
      contents: write
